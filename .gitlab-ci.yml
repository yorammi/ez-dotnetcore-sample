image: mcr.microsoft.com/dotnet/aspnet:7.0-alpine

stages:
  - build
  - test

build:
  stage: build
  script:
    - dotnet restore
    - dotnet build
  artifacts:
    paths:
      - ./**/dotnetcore-sample.dll
    expire_in: 1 day

test:
  stage: test
  script:
    - dotnet test
